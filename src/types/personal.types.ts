export interface Experience {
  title: string;
  company: string;
  description: string;
  startDate: string;
  endDate: string;
  task: string[];
}

export interface Education {
  title: string;
  institute: string;
  date: string;
  description?: string;
}
